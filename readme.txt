
DSA lab W-2

Mam Shifa Naveed

You need some extention to run the code 

1. CMake(additional)
2. Makefile Tools (requirde)
3. CMake Language Support(additional)
4. CMake Tools (requirde)
5. Code Runner(requirde)

just direct your self to project directory and run these command through 
your terminal or VSCode terminal

#Compile: Run (make) to compile all source files and create the executable.
#Execute: Run (make run) to compile (if necessary) and then execute main.
#Clean: Run (make clear) to remove the compiled files.
